class BL_Constants
{
	static const float GROUND_SPLATTER_INTERSECTION_DISTANCE = 3;
	static const float SURFACE_SPLATTER_INTERSECTION_DISTANCE = 2;
	static const float DECAL_FAR_PLANE = 2.0;
	static const float DECAL_LIFETIME_SECS = -1; // use `-1` for infinite lifetime decals.
}